{
  "name": "votepath-ai-server",
  "version": "1.0.0",
  "description": "VotePath AI Backend Server",
  "main": "server/server.js",
  "scripts": {
    "build": "npm install",
    "dev": "node server/server.js",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}